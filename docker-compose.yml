services:
  php-cli:
    build: ./docker/php-cli
    container_name: phpunit-coverage-tools-php-cli
    environment:
      - PHP_CS_FIXER_IGNORE_ENV=true
    volumes:
      - ./:/var/www/
    working_dir: /var/www

